---
phase: 05-settings-window
plan: 01
type: execute
depends_on: []
files_modified: [clipass/clipassApp.swift, clipass/Views/ClipboardPopup.swift, clipass/Views/SettingsView.swift, clipass/Views/RulesView.swift, clipass/Views/HooksView.swift, clipass/Views/RuleEditorView.swift, clipass/Views/HookEditorView.swift]
---

<objective>
Move Rules and Hooks configuration from popup inline navigation to a dedicated Settings window.

Purpose: Fix UX issues where inline navigation in MenuBarExtra popup doesn't work well (sheets overlay, dismiss closes menu). Settings window provides proper macOS-native experience for configuration.

Output:
- New Settings window with tabs for Rules and Hooks
- Simplified ClipboardPopup with just history and buttons to open Settings
- Proper navigation in Settings window using standard SwiftUI patterns
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files to modify:**
@clipass/clipassApp.swift
@clipass/Views/ClipboardPopup.swift
@clipass/Views/RulesView.swift
@clipass/Views/HooksView.swift
@clipass/Views/RuleEditorView.swift
@clipass/Views/HookEditorView.swift

**Decisions from prior phases:**
- Phase 3: RulesView/RuleEditorView patterns established
- Phase 4: HooksView/HookEditorView follow same patterns
- Current: Inline navigation with onBack/onDismiss callbacks doesn't work well in MenuBarExtra

**Issue being addressed:**
- Sheets in MenuBarExtra.window style overlay without proper dismiss
- Inline navigation is awkward in the small popup
- Need standard macOS Settings window for configuration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsView with TabView for Rules and Hooks</name>
  <files>clipass/Views/SettingsView.swift</files>
  <action>
Create new SettingsView.swift with:
- TabView containing two tabs: "Rules" and "Hooks"
- Use .tabItem with Label for each tab (wand.and.stars for Rules, bolt for Hooks)
- Embed RulesSettingsView and HooksSettingsView (will be refactored versions)
- Standard window size (~500x400)
- Remove the onBack callbacks from RulesView/HooksView - they'll use standard navigation

Note: Settings window has proper sheet support, so the existing sheet-based editors will work correctly here.
  </action>
  <verify>File created with TabView structure, compiles without errors</verify>
  <done>SettingsView.swift exists with TabView containing Rules and Hooks tabs</done>
</task>

<task type="auto">
  <name>Task 2: Refactor RulesView and HooksView for Settings window</name>
  <files>clipass/Views/RulesView.swift, clipass/Views/HooksView.swift, clipass/Views/RuleEditorView.swift, clipass/Views/HookEditorView.swift</files>
  <action>
Update RulesView:
- Remove `onBack` parameter and back button
- Remove `RulesScreen` enum and inline navigation
- Use standard .sheet() for RuleEditorView (works in Settings window)
- Keep the list, add button, reset button
- Add proper title in toolbar

Update HooksView:
- Remove `onBack` parameter and back button
- Remove `HooksScreen` enum and inline navigation
- Use standard .sheet() for HookEditorView (works in Settings window)
- Keep the list and add button
- Add proper title in toolbar

Update RuleEditorView and HookEditorView:
- Restore @Environment(\.dismiss) instead of onDismiss callback
- Remove onDismiss parameter
- Use dismiss() for Cancel and Save actions
  </action>
  <verify>All files compile, sheet navigation works in isolation</verify>
  <done>Views refactored to use standard SwiftUI sheet navigation without callbacks</done>
</task>

<task type="auto">
  <name>Task 3: Add Settings scene and simplify ClipboardPopup</name>
  <files>clipass/clipassApp.swift, clipass/Views/ClipboardPopup.swift</files>
  <action>
Update clipassApp.swift:
- Add Settings scene with SettingsView
- Pass modelContext to Settings scene

Update ClipboardPopup.swift:
- Remove PopupScreen enum and currentScreen state
- Remove inline RulesView/HooksView switching
- Keep rules/hooks buttons but change action to open Settings window
- Use NSApp.sendAction to open Settings: `NSApp.sendAction(Selector(("showSettingsWindow:")), to: nil, from: nil)`
- Keep the mainView content as the only view (history list)
- Remove the Group/switch structure

The popup should now just show:
- Header with title + Rules button + Hooks button (both open Settings)
- Search field
- History list
- Clear All / Quit buttons
  </action>
  <verify>Build succeeds, app launches, clicking Rules/Hooks opens Settings window</verify>
  <done>Settings window opens from popup, Rules/Hooks configuration works in Settings window with proper sheet navigation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -scheme clipass -destination 'platform=macOS' build` succeeds
- [ ] App launches with menu bar icon
- [ ] Clicking Rules button in popup opens Settings window to Rules tab
- [ ] Clicking Hooks button in popup opens Settings window to Hooks tab
- [ ] Adding/editing rules works with sheet presentation
- [ ] Adding/editing hooks works with sheet presentation
- [ ] Cancel button in editors dismisses sheet (not entire app)
- [ ] Popup remains lightweight (just history)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No build errors or warnings
- Settings window provides proper macOS configuration experience
- Popup is simplified to just clipboard history
- Sheet navigation works correctly in Settings window
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings-window/05-01-SUMMARY.md`
</output>
