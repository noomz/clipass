---
phase: 02-history
plan: 02
type: execute
depends_on: ["02-01"]
files_modified: [clipass/Views/ClipboardPopup.swift, clipass/Views/HistoryItemRow.swift]
---

<objective>
Add search filtering and click-to-paste functionality to the clipboard history popup.

Purpose: Users need to quickly find past clipboard items and paste them without manual copy-paste. Search makes large histories usable, quick paste is the core interaction.
Output: Search field filters history in real-time, clicking an item copies it to clipboard and optionally pastes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-history/02-01-SUMMARY.md

**From Plan 02-01:**
- SwiftData persistence working
- @Query fetches ClipboardItem sorted by timestamp
- Full scrollable history in popup

**Tech stack available:**
- SwiftUI, SwiftData
- @Query with predicates for filtering

**Key files:**
@clipass/Views/ClipboardPopup.swift
@clipass/Models/ClipboardItem.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search field with real-time filtering</name>
  <files>clipass/Views/ClipboardPopup.swift</files>
  <action>
1. Add search state:
   - @State private var searchText = ""

2. Add search field in header:
   - TextField with "Search..." placeholder
   - Use .textFieldStyle(.roundedBorder) for macOS look
   - Position below title, above history list

3. Filter history based on search:
   - Use @Query with dynamic predicate based on searchText
   - Filter: content.localizedCaseInsensitiveContains(searchText)
   - When searchText is empty, show all items
   - SwiftData approach: use #Predicate with the search term

4. Show "No results" state when search has no matches

**Avoid:**
- Client-side filtering of all items (use SwiftData predicate for efficiency)
- Debouncing (SwiftData queries are fast enough for real-time)
  </action>
  <verify>swift build succeeds, type in search field, results filter in real-time</verify>
  <done>Search field visible, typing filters history case-insensitively, empty search shows all, no matches shows empty state</done>
</task>

<task type="auto">
  <name>Task 2: Add click-to-paste and item actions</name>
  <files>clipass/Views/ClipboardPopup.swift, clipass/Views/HistoryItemRow.swift</files>
  <action>
1. Extract history item to separate view:
   - Create clipass/Views/HistoryItemRow.swift
   - Move item display logic (content preview, source app, timestamp)
   - Accept ClipboardItem as parameter

2. Make items clickable to copy:
   - Wrap item in Button
   - On click: NSPasteboard.general.setString(item.content, forType: .string)
   - Visual feedback: highlight on hover (.onHover modifier)

3. Add delete action:
   - Add trailing swipe action or context menu with "Delete"
   - On delete: modelContext.delete(item)

4. Add "Clear All" button:
   - Position in header or footer
   - Confirm before deleting (use .confirmationDialog)
   - Delete all items from SwiftData

**Avoid:**
- Auto-pasting (Cmd+V) after copying — user may not want immediate paste
- Closing popup after copy — user may want to copy multiple items
  </action>
  <verify>swift build succeeds, click item copies to clipboard, delete removes item, clear all works with confirmation</verify>
  <done>Clicking item copies to clipboard, hover shows highlight, delete removes single item, clear all with confirmation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] Search field visible and filters history
- [ ] Clicking an item copies its content to clipboard
- [ ] Hover effect visible on items
- [ ] Can delete individual items
- [ ] "Clear All" works with confirmation dialog
- [ ] Empty states handled (no results, no history)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Search filters history in real-time
- Click-to-copy works reliably
- Delete and clear functionality working
- Phase 2 complete — history feature fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-history/02-02-SUMMARY.md`
</output>
