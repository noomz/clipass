---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [clipass/clipassApp.swift, clipass/Views/ClipboardPopup.swift, clipass/Models/ClipboardItem.swift, clipass/Info.plist, Package.swift]
---

<objective>
Create the clipass Xcode project with menu bar icon and basic popup UI.

Purpose: Establish the SwiftUI app structure with MenuBarExtra scene. This is the foundation all other features build on.
Output: Working menu bar app that shows a popup when clicked, with Quit button.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

**Tech stack from research:**
- SwiftUI MenuBarExtra (macOS 13+)
- .menuBarExtraStyle(.window) for custom popup content

**Don't hand-roll:**
- Menu bar scaffold — use MenuBarExtra, not NSStatusItem

**Pitfalls to avoid:**
- Missing Quit button (LSUIElement hides from Dock)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with Swift Package Manager</name>
  <files>Package.swift, clipass/clipassApp.swift, clipass/Info.plist</files>
  <action>
Create a new macOS app project structure:

1. Create Package.swift with:
   - macOS 14.0+ deployment target
   - KeyboardShortcuts dependency: `.package(url: "https://github.com/sindresorhus/KeyboardShortcuts", from: "2.0.0")`
   - clipass executable target

2. Create clipass/clipassApp.swift with:
   - @main struct clipassApp: App
   - MenuBarExtra scene with systemImage "doc.on.clipboard"
   - .menuBarExtraStyle(.window)
   - Placeholder ClipboardPopup view

3. Create clipass/Info.plist with:
   - LSUIElement = true (hide from Dock)
   - CFBundleName = clipass
   - Minimum macOS version 14.0

**Avoid:** Using Xcode project file (.xcodeproj) — use Swift Package Manager for simpler structure.
  </action>
  <verify>swift build compiles without errors</verify>
  <done>Project structure exists, swift build succeeds, Package.swift has KeyboardShortcuts dependency</done>
</task>

<task type="auto">
  <name>Task 2: Create popup UI with Quit button</name>
  <files>clipass/Views/ClipboardPopup.swift, clipass/Models/ClipboardItem.swift</files>
  <action>
1. Create clipass/Models/ClipboardItem.swift:
   - struct ClipboardItem: Identifiable
   - Properties: id (UUID), content (String), sourceApp (String?), timestamp (Date)

2. Create clipass/Views/ClipboardPopup.swift:
   - VStack with placeholder "Clipboard History" text
   - Divider
   - Quit button at bottom: `Button("Quit clipass") { NSApplication.shared.terminate(nil) }`
   - Add .keyboardShortcut("q", modifiers: .command) to Quit button
   - Frame with reasonable size (width: 300, minHeight: 200)

**Avoid:** Complex UI — this is just the shell. History UI comes in Phase 2.
  </action>
  <verify>swift build succeeds, swift run launches app with menu bar icon visible</verify>
  <done>Menu bar icon appears, clicking shows popup with Quit button, Cmd+Q quits app</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift build` succeeds without errors
- [ ] `swift run` launches app
- [ ] Menu bar icon (clipboard) appears in menu bar
- [ ] Clicking icon shows popup window
- [ ] Quit button visible and functional
- [ ] Cmd+Q keyboard shortcut works
- [ ] App does NOT appear in Dock (LSUIElement working)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Working menu bar app with popup and Quit functionality
- Ready for Plan 02 (clipboard monitoring)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
