---
phase: 04-hooks
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [clipass/Views/HooksView.swift, clipass/Views/HookEditorView.swift, clipass/Views/ClipboardPopup.swift]
---

<objective>
Create hooks management UI for viewing, adding, editing, and deleting hooks.

Purpose: Allow users to configure automation hooks through the app UI
Output: HooksView listing hooks, HookEditorView for editing, integration in ClipboardPopup
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-hooks/04-01-SUMMARY.md

# Key source files (existing UI patterns):
@clipass/Views/RulesView.swift
@clipass/Views/RuleEditorView.swift
@clipass/Views/ClipboardPopup.swift
@clipass/Models/Hook.swift

**Tech stack available:** SwiftUI, SwiftData
**Established patterns:**
- RulesView: List with ForEach, inline toggles via @Bindable, swipe/context menu delete
- RuleEditorView: Sheet with form fields, regex validation, live testing
- ClipboardPopup: Toolbar button with count badge, sheet navigation
- @Bindable for inline property updates without opening editor

**Constraining decisions:**
- Follow RulesView/RuleEditorView structure for consistency
- Hooks button in popup alongside Rules button
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HooksView for listing hooks</name>
  <files>clipass/Views/HooksView.swift</files>
  <action>Create HooksView matching RulesView structure:
  - @Environment(\.modelContext) and @Query for Hook items sorted by order
  - List with ForEach showing each hook
  - HookRow subview with:
    - Hook name as primary text
    - Command preview (truncated) as secondary text
    - Toggle for isEnabled (using @Bindable pattern)
    - Badge showing sourceAppFilter if set (extract app name from bundle ID)
  - Swipe action and context menu for delete
  - Toolbar with + button to add new hook
  - NavigationStack wrapping the list
  - Sheet for HookEditorView (add/edit)

  Follow RulesView closely for consistency. No Reset to Defaults for hooks (user-defined only).</action>
  <verify>Build succeeds</verify>
  <done>HooksView.swift with list, toggle, delete, add button, sheet navigation</done>
</task>

<task type="auto">
  <name>Task 2: Create HookEditorView for add/edit</name>
  <files>clipass/Views/HookEditorView.swift</files>
  <action>Create HookEditorView matching RuleEditorView structure:
  - Form with sections:
    - Name (TextField, required)
    - Pattern (TextField, optional - empty matches all)
    - Command (TextField, required - the shell command to run)
    - Source App Filter (TextField, optional bundle ID)
    - Enabled toggle
  - Pattern validation: if not empty, show regex validity indicator
  - Command hint: "Receives CLIPASS_CONTENT and CLIPASS_SOURCE_APP environment variables"
  - Cancel and Save buttons (Save disabled if name or command empty)
  - Support both add (new Hook) and edit (existing Hook) modes

  For edit mode, accept optional Hook binding. For add mode, create new Hook on save.</action>
  <verify>Build succeeds</verify>
  <done>HookEditorView.swift with form, validation, save/cancel</done>
</task>

<task type="auto">
  <name>Task 3: Add Hooks button to ClipboardPopup</name>
  <files>clipass/Views/ClipboardPopup.swift</files>
  <action>Add Hooks access in ClipboardPopup toolbar:
  - Add @Query for Hook.self to get count
  - Add @State for showingHooks sheet
  - Add toolbar button with hook icon (e.g., "bolt" or "terminal") and count badge
  - Position after Rules button
  - Present HooksView as sheet when tapped

  Follow the same pattern used for Rules button.</action>
  <verify>Build and run. Open popup, verify Hooks button appears with count. Tap to open HooksView. Add a hook, verify it saves and count updates.</verify>
  <done>Hooks button in popup toolbar, opens HooksView sheet, shows hook count</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project clipass.xcodeproj -scheme clipass build` succeeds
- [ ] HooksView.swift exists with list and CRUD operations
- [ ] HookEditorView.swift exists with form and validation
- [ ] ClipboardPopup shows Hooks button with count badge
- [ ] Can add, edit, toggle, and delete hooks through UI
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No compiler errors or warnings
- UI consistent with Rules management (same patterns)
- Full CRUD for hooks via UI
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-hooks/04-02-SUMMARY.md` with:
- Phase 4: Hooks complete status
- Ready for v1 release or additional features
</output>
